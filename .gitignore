# Rust / Soroban
target/
**/target/
*.wasm
.stellar/

# Node / JS
node_modules/
.next/
dist/

# Secrets & env
.env
.env.*
!.env.*.example
*.key
deployments/*.secret

# Vendored upstream (pinned by SHA in spike/versions.lock — not committed)
vendor/

# ZK artifacts (large; regenerated)
*.zkey
*.ptau
*.r1cs
*.wtns
circuits/build/

# OS / editor
.DS_Store
*.swp

# Foundry deps (re-install via 'forge install')
spike/sol/lib/
evm-tree/lib/

# Local Claude Code skills/config — not part of the project
.claude/

# Claude Code tooling lockfile (not project code)
skills-lock.json
