# Rust / Soroban
target/
**/target/
*.wasm
.stellar/

# Node / JS
node_modules/
.next/
dist/

# Secrets & env
.env
.env.*
!.env.*.example
*.key
legacy/deployments/*.secret

# Vendored upstream (pinned by SHA in spike/versions.lock — not committed)
vendor/

# ZK artifacts (large; regenerated) — legacy bridge
*.zkey
*.ptau
*.r1cs
*.wtns
legacy/circuits/build/

# OS / editor
.DS_Store
*.swp

# Foundry deps (re-install via 'forge install') — legacy bridge
legacy/spike/sol/lib/
legacy/evm-tree/lib/

# Local scratch / exploration outputs — not project source
idea-*.html
validation-report.html
backing-state.json
logs/

# Local Claude Code skills/config — not part of the project
.claude/

# Subagent-driven-development scratch (progress ledger, briefs, reports)
.superpowers/

# Claude Code tooling lockfile (not project code)
skills-lock.json

# Internal design docs / plans — not published to remote
docs/
